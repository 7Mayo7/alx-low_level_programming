#include <stdio.h>

/**
 * main - the entry point
 * decription c program to printf function
 * Return: value 0 the program is success
 */

int main(void)

{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

