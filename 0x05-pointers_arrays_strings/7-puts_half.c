#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *puts_half - prints half of a string followed by a new line
 *@str: take the input
 *Return: void
 */
void puts_half(char *str)
{
	int a;

	for (a = 0; str[a] != '\0'; ++a)

	a++;

	for (a /= 2; str[a] != '\0'; ++a)
	{

	int _iputchar('str'[a]);

	}
	putchar('\n');
}
